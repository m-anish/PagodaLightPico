"""
Configuration file for PagodaLightPico project.

Rename this file to `config.py` before use, and update the values with your specific settings.

Attributes:
    WIFI_SSID (str): WiFi network SSID for Pico W connection.
    WIFI_PASSWORD (str): WiFi network password.
    TIMEZONE_NAME (str): Abbreviation for the timezone (e.g., IST, UTC, PST).
    TIMEZONE_OFFSET (float): Offset from UTC in hours (e.g., 5.5 for IST).
    RTC_I2C_SDA_PIN (int): GPIO pin number for I2C SDA line connected to DS3231.
    RTC_I2C_SCL_PIN (int): GPIO pin number for I2C SCL line connected to DS3231.
    LOG_LEVEL (str): Logging level. Options: 'FATAL', 'ERROR', 'WARN', 'INFO', 'DEBUG'.
"""

# WiFi credentials
WIFI_SSID = "your_wifi_ssid_here"
WIFI_PASSWORD = "your_wifi_password_here"

# Timezone settings
TIMEZONE_NAME = "IST"       # e.g., IST, UTC, PST
TIMEZONE_OFFSET = 5.5       # Timezone offset from UTC in hours (e.g., 5.5 for IST)

# I2C pins for DS3231 RTC (adjust as per your wiring)
RTC_I2C_SDA_PIN = 20
RTC_I2C_SCL_PIN = 21

# Logging level (options: 'FATAL', 'ERROR', 'WARN', 'INFO', 'DEBUG')
LOG_LEVEL = 'DEBUG'
